FROM python:3.11-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends strace \
    && rm -rf /var/lib/apt/lists/*

# Non-root execution is enforced by AgentSandbox (user 1000:1000).
